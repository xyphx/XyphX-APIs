<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>XyphX - New Application Received</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      width: 100%;
      overflow: hidden;
      background: linear-gradient(135deg, #1b003a, #2e006d);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: #e0d7ff;
    }

    .container {
      background: #2a0057;
      border-radius: 0;
      box-shadow: none;
      width: 100vw;
      min-height: 100vh;
      padding: 60px 5vw 70px;
      box-sizing: border-box;
      text-align: center;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .logo {
      width: 100px;
      height: 100px;
      margin: 0 auto 30px;
      border-radius: 50%;
      border: 3px solid #bb86fc;
      box-shadow: 0 0 20px #bb86fc;
      overflow: hidden;
      background: #3a0075;
    }

    .logo img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    h2 {
      font-weight: 700;
      font-size: 2.2rem;
      margin-bottom: 25px;
      color: #d0bfff;
      text-shadow: 0 0 5px #bb86fc;
    }

    .info {
      text-align: left;
      background: #3b007a;
      border-radius: 12px;
      padding: 20px 25px;
      box-shadow: inset 0 0 10px #5500cc;
      font-size: 1.1rem;
      line-height: 1.6;
      color: #e0d7ff;
      max-width: 800px;
      margin: 0 auto;
    }

    .info p {
      margin: 12px 0;
    }

    .info strong {
      color: #bb86fc;
      display: inline-block;
      width: 140px;
    }

    a.portfolio-link {
      color: #d9b3ff;
      text-decoration: none;
      font-weight: 600;
      word-break: break-word;
      transition: color 0.3s ease;
    }

    a.portfolio-link:hover {
      color: #fff;
      text-decoration: underline;
    }

    @media (max-width: 480px) {
      .container {
        padding: 30px 20px 40px;
      }

      .info strong {
        width: 120px;
      }

      h2 {
        font-size: 1.8rem;
      }
    }
  </style>
</head>
<body>
  <div class="container" role="main">
    <div class="logo" aria-label="Company Logo">
      <img src="https://avatars.githubusercontent.com/u/205357654?v=4" alt="Company Logo" />
    </div>
    <h2>New Application Received</h2>
    <div class="info">
      <p><strong>Name:</strong> {{ name }}</p>
      <p><strong>Organization:</strong> {{ org_name }}</p>
      <p><strong>Technical Interests:</strong> {{ tech_interest }}</p>
      <p><strong>Phone:</strong> {{ phone }}</p>
      <p><strong>Email:</strong> {{ email }}</p>
      {% if portfolio %}
      <p><strong>Portfolio:</strong> <a href="{{ portfolio }}" class="portfolio-link" target="_blank" rel="noopener noreferrer">{{ portfolio }}</a></p>
      {% endif %}
    </div>
  </div>
</body>
</html>
